// Test stub for the `server-only` package. The real module throws when loaded
// outside a React Server Component; aliasing it here lets us unit-test server
// modules (queries, mutations) in the vitest (jsdom) environment.
export {};
