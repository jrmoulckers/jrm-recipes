# studio:base:start
# synced from jrmoulckers/.github — canonical source; do not edit here
# Normalize every detected text file to LF in the index and working tree.
# Git's automatic text detection leaves binary files byte-for-byte unchanged.
* text=auto eol=lf
# studio:base:end

# Repo-specific overrides go BELOW the managed region above. Git resolves attributes
# by LAST matching pattern, and the canonical stanza's * matches every path, so a
# more-specific rule only takes effect if it follows the wildcard.
#
# That ordering is guaranteed by the sync engine, not by how this file was seeded:
# canon is *prepended* for .gitattributes (Markdown targets are appended instead).
# See ADR-0011 in jrmoulckers/.github. Do not move local rules above the region.
